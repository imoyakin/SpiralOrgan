api_version = "spiralorgan.policy/v0.1"
mode = "supervised"

[features.channels]
enabled = true

[features.plugins]
enabled = true

[features.sandbox]
enabled = true
backend = "auto"

[features.pipeline_edit]
enabled = true

[features.token_distill]
enabled = false

[features.vision_snapshot]
enabled = false

[features.worlddefine_bridge]
enabled = false

[policy]
workspace_only = true
allowed_commands = ["git", "cargo", "npm", "pnpm", "ls", "cat", "rg", "sed"]
forbidden_paths = ["/etc", "/root", "/proc", "/sys", "~/.ssh", "~/.gnupg", "~/.aws"]

[policy.risk_matrix]
shell_exec = "high"
file_write_outside_workspace = "critical"
browser_remote_control = "high"
channel_bind = "medium"
plugin_install = "high"

[policy.enforcement]
low = "allow"
medium = "approve"
high = "approve"
critical = "deny"

[policy.approval]
required_for = ["medium", "high"]

[policy.approval.channel_mode]
cli = "interactive"
telegram = "operator_only"
webhook = "token_and_operator"

[policy.rate_limits]
per_user_per_minute = 20
per_channel_per_minute = 120
per_session_parallel_actions = 4

[audit]
enabled = true
redact_secrets = true
export_format = "jsonl"

